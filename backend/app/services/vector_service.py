# This is for your AI's memory (RAG).

# Functions: chunk_text(text), get_embeddings(text_chunk), save_to_vector_db(vector).

# Why: Legal documents are too long for an AI to read all at once. This service chops them into "bite-sized" pieces so the AI can find exactly the right paragraph later.